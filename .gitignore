node_modules/
pipeline/build/
# pipeline/upstream/ is tracked — clone includes it, no npm pack needed
pipeline/.upgrade-tmp/
# varmap-*.json kept tracked — ci-upgrade diffs old vs new for rename sweep
*.tgz
.env
dist/
cli
cli-dev
.DS_Store
# .claude/ is maintainer-local — skills, settings, state never ship to users
.claude/
.knowledge-graph/
.mcp.json
.playwright/
.playwright-mcp/
