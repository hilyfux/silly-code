node_modules/
pipeline/build/
# pipeline/upstream/ is tracked — clone includes it, no npm pack needed
pipeline/.upgrade-tmp/
# varmap-*.json kept tracked — ci-upgrade diffs old vs new for rename sweep
*.tgz
.env
dist/
cli
cli-dev
.DS_Store
# .claude/: ignore personal state, but track team-shared skills + settings.json
.claude/*
!.claude/skills/
!.claude/settings.json
.claude/settings.local.json
.claude/worktrees/
.knowledge-graph/
.mcp.json
.playwright/
.playwright-mcp/
