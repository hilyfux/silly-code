# docs/memory/ — Memory System Documentation
## Prohibitions
- Referencing bun test or v1 source paths → project uses node tests and patch pipeline
## When Changing
- Example commands → must match current v2 workflow (node tests/*.cjs, silly commands)
## Conventions
- User-facing documentation for Claude Code memory system
- Examples should use silly-code specific commands, not upstream claude-code
