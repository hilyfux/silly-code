/**
 * MonitorPermissionRequest — stub for source-mode compatibility.
 * Required by: src/components/permissions/PermissionRequest.tsx (MONITOR_TOOL flag)
 */
import * as React from 'react'

export function MonitorPermissionRequest(_props: any): React.ReactNode {
  return null
}
