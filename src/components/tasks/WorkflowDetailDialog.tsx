/**
 * WorkflowDetailDialog — stub for source-mode compatibility.
 * Required by: src/components/tasks/BackgroundTasksDialog.tsx (WORKFLOW_SCRIPTS flag)
 */
import * as React from 'react'

export function WorkflowDetailDialog(_props: any): React.ReactNode {
  return null
}
