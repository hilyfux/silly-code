/**
 * Job classifier — stub for source-mode compatibility.
 * Required by: src/query/stopHooks.ts (TEMPLATES flag)
 */

export async function classifyAndWriteState(
  _jobDir: string | undefined,
  _assistantMessages: unknown[],
): Promise<void> {}
