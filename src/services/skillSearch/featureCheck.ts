/**
 * Skill search feature check — stub for source-mode compatibility.
 */

export function isSkillSearchEnabled(): boolean {
  return false
}

export function isRemoteSkillSearchEnabled(): boolean {
  return false
}
