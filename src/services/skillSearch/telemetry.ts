/**
 * Skill search telemetry — stub for source-mode compatibility.
 */

export function logSkillSearchEvent(
  _event: string,
  _metadata?: Record<string, unknown>,
): void {}

export function logSkillInvocation(
  _skillName: string,
  _metadata?: Record<string, unknown>,
): void {}
