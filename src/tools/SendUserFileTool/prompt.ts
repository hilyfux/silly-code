/**
 * SendUserFileTool prompt — stub for source-mode compatibility.
 * Required by: src/components/Messages.tsx (KAIROS flag)
 */

export const SEND_USER_FILE_TOOL_NAME = 'send_user_file'

export function getSendUserFileToolPrompt(): string {
  return ''
}
