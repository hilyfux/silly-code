/**
 * WorkflowPermissionRequest — stub for source-mode compatibility.
 * Required by: src/components/permissions/PermissionRequest.tsx (WORKFLOW_SCRIPTS flag)
 */
import * as React from 'react'

export function WorkflowPermissionRequest(_props: any): React.ReactNode {
  return null
}
